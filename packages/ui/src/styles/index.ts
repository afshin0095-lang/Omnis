/**
 * Structural CSS owned by the component library.
 *
 * Themes supply values; this supplies the few things a value cannot express —
 * keyframes, the focus ring, and the reduced-motion override.
 */

export {
  ANIMATED_CLASS,
  ANIMATIONS,
  BASE_STYLE_ATTRIBUTE,
  FOCUSABLE_CLASS,
  hasBaseStyles,
  injectBaseStyles,
  OMNIS_BASE_CSS,
  removeBaseStyles,
} from "./base.js";
export type { AnimationName } from "./base.js";
